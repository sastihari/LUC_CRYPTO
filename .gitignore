# ============================================================
# IGNORE EVERYTHING — only encrypted archive goes to GitHub
# ============================================================
*

# Allow only the encrypted file and gitignore itself
!.gitignore
!LUC_CRYPTO_encrypted.tar.gz.gpg
